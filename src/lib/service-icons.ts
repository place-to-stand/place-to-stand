import {
  BarChart3,
  Code2,
  Compass,
  GitBranch,
  ServerCog,
  type LucideIcon,
} from 'lucide-react'

// Maps a Service.icon name (string) to its lucide component.
export const serviceIcons: Record<string, LucideIcon> = {
  Code2,
  GitBranch,
  BarChart3,
  Compass,
  ServerCog,
}
