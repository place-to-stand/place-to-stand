##############
#   PORTAL   #
##############

# Both forms are delivered through the portal: it records the submission and
# sends the team notification and the visitor's confirmation. This site sends
# no email and holds no Resend key. With these unset, local dev logs the
# payload and reports success; production shows the visitor an error.

AUDIT_INTAKE_TOKEN=
CONTACT_INTAKE_TOKEN=

PORTAL_API_BASE_URL=
